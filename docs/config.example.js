/* Template for config.js (which is gitignored).
     cp config.example.js config.js
   Note: whatever ends up here is downloaded by the browser, so it can
   only ever hold values that are safe to be public. */
window.HUB_CONFIG = {
  contactEndpoint: "https://api.web3forms.com/submit",
  contactKey: "YOUR_WEB3FORMS_ACCESS_KEY"
};
